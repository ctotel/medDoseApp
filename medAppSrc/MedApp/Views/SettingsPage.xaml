<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MedApp.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:MedApp.ViewModels"
    x:DataType="vm:SettingsViewModel"
    Title="Settings">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="16">

            <Label FontAttributes="Bold" FontSize="22" Text="Settings" />

            <!-- ── Snooze duration ────────────────────────────────── -->
            <Label FontAttributes="Bold" FontSize="16" Text="Notifications" />
            <Grid ColumnDefinitions="*,Auto,Auto" Padding="0">
                <Label Grid.Column="0" Text="Snooze duration" VerticalOptions="Center" />
                <Label Grid.Column="1"
                       VerticalOptions="Center"
                       WidthRequest="50"
                       HorizontalTextAlignment="Center">
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="{Binding SnoozeDuration}" FontAttributes="Bold" />
                            <Span Text=" min" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>
                <Stepper Grid.Column="2"
                         Increment="5"
                         Maximum="60"
                         Minimum="5"
                         Value="{Binding SnoozeDuration}" />
            </Grid>

            <!-- ── Quiet hours ────────────────────────────────────── -->
            <Grid ColumnDefinitions="*,Auto">
                <Label Grid.Column="0" Text="Quiet hours" VerticalOptions="Center" />
                <Switch Grid.Column="1" IsToggled="{Binding QuietHoursEnabled}" />
            </Grid>

            <Grid ColumnDefinitions="*,*" IsVisible="{Binding QuietHoursEnabled}" Padding="16,0,0,0">
                <HorizontalStackLayout Grid.Column="0" Spacing="8">
                    <Label Text="From:" VerticalOptions="Center" />
                    <TimePicker Time="{Binding QuietStart}" />
                </HorizontalStackLayout>
                <HorizontalStackLayout Grid.Column="1" Spacing="8">
                    <Label Text="To:" VerticalOptions="Center" />
                    <TimePicker Time="{Binding QuietEnd}" />
                </HorizontalStackLayout>
            </Grid>

            <!-- ── Notification permission ────────────────────────── -->
            <BoxView HeightRequest="1" Color="{StaticResource PrimaryLight}" Margin="0,8" />
            <Grid ColumnDefinitions="*,Auto">
                <VerticalStackLayout Grid.Column="0">
                    <Label Text="Notification permission" FontAttributes="Bold" />
                    <Label Text="{Binding PermissionStatus}" FontSize="13" TextColor="{StaticResource Muted}" />
                </VerticalStackLayout>
                <Button Grid.Column="1"
                        Text="Grant"
                        BackgroundColor="{StaticResource Primary}"
                        TextColor="White"
                        HeightRequest="36"
                        FontSize="13"
                        Padding="16,0"
                        CornerRadius="18"
                        Command="{Binding RequestPermissionCommand}" />
            </Grid>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
